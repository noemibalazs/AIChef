package com.noemi.aichef.util

object Constants {

    const val KEY_PARAMETER = "key"
    const val BODY_TEXT = "The output should be in an array and each object should contain a recipe name field named name, description field named description, array of ingredients named ingredients, and array of instructions named steps."

    const val CHEF_TABLE = "ai_chef_table"
    const val CHEF_DB = "ai_chef_db"

    const val KEY_RECIPE = "key_recipe"
}